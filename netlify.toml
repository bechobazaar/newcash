[build]
  publish = "."
  command = ""

[functions]
  node_bundler = "esbuild"
  included_files = ["netlify/functions/_firebaseAdmin.js"]


[build.environment]
  NODE_VERSION = "18"

[[functions]]
  name = "autobump"
  schedule = "*/5 * * * *"   # run every 5 minutes

