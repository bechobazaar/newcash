[build]
  publish = "."
  command = ""

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"

[build.environment]
  NODE_VERSION = "18"

# (optional) only if this function file actually exists
[functions."auto-bump"]
  schedule = "*/10 * * * *"
