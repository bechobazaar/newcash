[build]
  publish = "."
  command = ""

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"

[build.environment]
  NODE_VERSION = "18"

# 🚀 Autobump function har ghante chalega
[[scheduled.functions]]
  name = "autobump"
  cron = "*/2 * * * *"

